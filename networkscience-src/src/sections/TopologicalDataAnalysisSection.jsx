import { lazy } from 'react';import { K,KB } from '../components/Katex';import ArtifactWrapper from '../components/ArtifactWrapper';const PersistentHomologyExplorer=lazy(()=>import('../artifacts/PersistentHomologyExplorer'));const SimplexLadder=lazy(()=>import('../artifacts/SimplexLadder'));
export default function TopologicalDataAnalysisSection(){return <section id="tda" className="ns-section"><h2>22. Topological Data Analysis</h2><p>A <K l="k"/>-simplex is the convex hull of <K l="k+1"/> points in general position: a point, segment, filled triangle and tetrahedron for dimensions zero through three. Complexes glue simplices together.</p><ArtifactWrapper title="Simplex and Betti Ladder"><SimplexLadder/></ArtifactWrapper><h3>Betti numbers</h3><p><K l="\beta_0"/> counts connected components, <K l="\beta_1"/> independent loops and <K l="\beta_2"/> enclosed voids.</p><KB l="\beta_k=\operatorname{rank}(H_k)"/><p>The lecture's diamond complex has vertices a–d, five edges E–I and one filled upper triangle J. It has one component, one unfilled lower hole and no void: <K l="(1,1,0,0,\ldots)"/> with Poincaré polynomial <K l="1+x"/>. Reference shapes are a solid blob (1,0,0), sphere (1,0,1), three-holed blob (1,3,0) and torus (1,2,1).</p><h3>Persistent homology</h3><p>A Vietoris–Rips filtration increases <K l="\epsilon"/>, joining points within distance <K l="\epsilon"/> and filling every three-clique. Features are born and die; a barcode records <K l="[\mathrm{birth},\mathrm{death}]"/>. Long intervals are robust structure, short ones sampling noise, so no single scale must be chosen.</p><KB l="\beta_1=\dim Z_1-\dim B_1=(E-V+\beta_0)-\operatorname{rank}(\partial_2)"/><ArtifactWrapper title="Interactive: Persistent Homology Explorer"><PersistentHomologyExplorer/></ArtifactWrapper><p>The live explorer computes <K l="\beta_0"/> exactly by union-find and <K l="\beta_1"/> over <K l="\mathrm{GF}(2)"/> from the triangle-to-edge boundary rank. It deliberately omits a live <K l="\beta_2"/> rather than displaying an uncomputed value.</p></section>}
