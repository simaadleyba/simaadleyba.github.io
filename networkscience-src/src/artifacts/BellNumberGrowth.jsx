import { ComposedChart,Line,XAxis,YAxis,Tooltip,ResponsiveContainer } from 'recharts';
function bell(n){const a=Array.from({length:n+1},()=>Array(n+1).fill(0));a[0][0]=1;const out=[1];for(let i=1;i<=n;i++){a[i][0]=a[i-1][i-1];for(let j=1;j<=i;j++)a[i][j]=a[i][j-1]+a[i-1][j-1];out.push(a[i][0])}return{a,out}}
export default function BellNumberGrowth(){const {a,out}=bell(25),data=out.slice(1).map((v,i)=>({n:i+1,b:Math.log10(v),pow:(i+1)*Math.log10(2),exp:(i+1)*Math.log10(Math.E)}));return <div><ResponsiveContainer width="100%" height={220}><ComposedChart data={data}><XAxis dataKey="n" tick={{fontSize:10}}/><YAxis tick={{fontSize:10}}/><Tooltip/><Line dataKey="b" name="log₁₀ Bell" stroke="#c1121f" dot={false}/><Line dataKey="pow" name="log₁₀ 2ⁿ" stroke="#4a90d9" dot={false}/><Line dataKey="exp" name="log₁₀ eⁿ" stroke="#2d6a4f" dot={false}/></ComposedChart></ResponsiveContainer><table><tbody>{a.slice(0,7).map((r,i)=><tr key={i}>{r.slice(0,i+1).map((v,j)=><td key={j}>{v}</td>)}</tr>)}</tbody></table></div>}
