import React from 'react';
import ReactDOM from 'react-dom/client';
import ModelEvaluationApp from './ModelEvaluationApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ModelEvaluationApp />
  </React.StrictMode>,
);
