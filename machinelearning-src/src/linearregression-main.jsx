import React from 'react';
import ReactDOM from 'react-dom/client';
import LinearRegressionApp from './LinearRegressionApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LinearRegressionApp />
  </React.StrictMode>,
);
