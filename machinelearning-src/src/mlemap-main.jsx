import React from 'react';
import ReactDOM from 'react-dom/client';
import MleMapApp from './MleMapApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <MleMapApp />
  </React.StrictMode>,
);
