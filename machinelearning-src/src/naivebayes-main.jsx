import React from 'react';
import ReactDOM from 'react-dom/client';
import NaiveBayesApp from './NaiveBayesApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <NaiveBayesApp />
  </React.StrictMode>,
);
