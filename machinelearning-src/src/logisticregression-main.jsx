import React from 'react';
import ReactDOM from 'react-dom/client';
import LogisticRegressionApp from './LogisticRegressionApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LogisticRegressionApp />
  </React.StrictMode>,
);
