import React from 'react';
import ReactDOM from 'react-dom/client';
import EnsembleLearningApp from './EnsembleLearningApp';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <EnsembleLearningApp />
  </React.StrictMode>,
);
